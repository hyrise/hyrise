#include "knapsack_solver.hpp"

std::vector<size_t> KnapsackSolver::solve(const uint64_t memory_budget, const std::vector<uint64_t> values, const std::vector<uint64_t> costs) {
  // returns indices of objects to pack
}